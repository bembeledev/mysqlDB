// calculos.super
var PI: float = 3.14159;

function calcular_imc(peso: float, altura: float) -> float {
    return peso / (altura * altura);
}

function saudacao(nome: string) -> string {
    return "Olá, " + nome + "! Bem-vindo ao sistema.";
}